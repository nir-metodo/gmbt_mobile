// Contact detail rendered INSIDE the chats stack, so pressing back from a contact
// opened from a conversation returns to that conversation (not the Contacts tab).
// Reuses the exact same screen component as /(tabs)/contacts/[id].
export { default } from '../../contacts/[id]';
